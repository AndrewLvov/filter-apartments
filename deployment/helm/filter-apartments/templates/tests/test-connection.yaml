apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "filterappartments-chart.fullname" . }}-test-connection"
  labels:
    {{- include "filterappartments-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "filterappartments-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
