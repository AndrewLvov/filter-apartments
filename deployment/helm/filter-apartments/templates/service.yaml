apiVersion: v1
kind: Service
metadata:
  name: {{ include "filterappartments-chart.fullname" . }}
  labels:
    {{- include "filterappartments-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "filterappartments-chart.selectorLabels" . | nindent 4 }}
